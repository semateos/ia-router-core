/**
 * Provides means to match strings (e.g. routes) against rules (routes).
 *
 * @module Router
 * @namespace Router
 * @main
 */
if (typeof(Router) === 'undefined') {
    Router        = {};
}
Router.Routes = Router.Routes || {};
