/**
 * Provides means to match strings (e.g. routes) against rules (routes).
 *
 * @module Router
 * @namespace InnoAccel.Router
 * @main
 */
if (typeof(InnoAccel) === 'undefined') {
    InnoAccel = {};
}
InnoAccel.Router ={};
